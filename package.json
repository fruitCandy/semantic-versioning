{
    "devDependencies": {
        "@commitlint/cli": "^6.2.0",
        "@commitlint/config-conventional": "^6.1.3",
        "@commitlint/config-lerna-scopes": "^6.1.3",
        "@commitlint/prompt-cli": "^6.1.3",
        "beta-changelog": "^0.1.1",
        "beta-cz": "^0.1.0",
        "commitizen": "^2.9.6",
        "husky": "^0.14.3",
        "lerna": "^3.0.0-beta.15"
    },
    "name": "semantic-versioning",
    "version": "1.0.0",
    "main": "index.js",
    "private": true,
    "scripts": {
        "test": "scripts/test",
        "commitmsg": "commitlint -e $GIT_PARAMS",
        "semantic-release": "lerna publish --conventional-commits --yes --changelog-preset beta-changelog",
        "bumpTest": "lerna publish --skip-npm --conventional-commits --changelog-preset beta-changelog",
        "commit": "git cz",
        "bootstrap": "lerna bootstrap"
    },
    "workspaces": [
        "packages/*"
    ],
    "config": {
        "commitizen": {
            "path": "./node_modules/beta-cz"
        }
    }
}
